# golangci-lint v2 -- https://golangci-lint.run/docs/configuration/file/
version: "2"

run:
  timeout: 3m

linters:
  default: standard
  enable:
    - gocritic
    - misspell
    - revive
    - unconvert
    - unparam
  exclusions:
    generated: strict

formatters:
  enable:
    - goimports
