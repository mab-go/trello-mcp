version: 2

builds:
  - main: ./cmd/trello-mcp
    binary: trello-mcp
    ldflags:
      - -X github.com/mab-go/trello-mcp/internal/version.Version={{.Version}}
      - -X github.com/mab-go/trello-mcp/internal/version.Commit={{.FullCommit}}
      - -X github.com/mab-go/trello-mcp/internal/version.Date={{.Date}}
    goos:
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
